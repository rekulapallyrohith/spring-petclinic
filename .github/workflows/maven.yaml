name: "Building package using Maven"
on: 
    push: 
        branches:
          - main
    workflow_dispatch:
jobs:
    maven:
        runs-on: ubuntu-latest
        steps:
          - name: Checkout repository
            uses: actions/checkout@v4
          - name: Build 
            run: mvn package
